

public class Calculator {
    public Integer abs(Integer x) {
        return Math.abs(x);
    }

    public Double pow(Integer a, Integer b) {

        return Math.pow(a, b);
    }

    public Double sqrt(Integer d) {

        return Math.sqrt(d);
    }
}
